(function($) {

$(document).ready(function() {	

  // if I put it in the begining of the file the plugin button doesn't work
  // Admin ------ start ---------------------
  // used by the admin option page
  $('.color-picker').wpColorPicker();
   // Admin ------ end --------------------- 
  
});
	
})( jQuery );